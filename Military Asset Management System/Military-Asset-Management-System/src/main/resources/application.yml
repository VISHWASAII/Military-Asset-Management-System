#Application name
spring:
  application:
    name: Military-Asset-Management-System

#Configuration for the database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db.pkdcrqdwqyfrjenuwcls.supabase.co:5432/postgres?user=postgres&password=Saivishwa13
    username: postgres
    password: Saivishwa13

#Configuration for the data jpa
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
